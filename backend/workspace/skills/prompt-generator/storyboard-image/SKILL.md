---
name: storyboard-image
description: Write Gemini still prompts that lock attached asset images.
---

# Gemini 分镜静帧提示词（分镜段落 → image_prompt）

这段文字会交给 **Gemini 图片模型**（`gemini-*-image` / Nano Banana）生成一张 16:9 单帧分镜图。

请求里资产图会 **写在文字前面**：`contents.parts` = 第 1 张图、第 2 张图…然后才是 `image_prompt`。Gemini 只认「第一张图 / 第二张图」，不认 MiniMax 的 `@角色名`，也不认视频 Omni 的 `<IMAGE_REF_N>`。

它不是视频提示词：不要时间轴、不要「0-3秒」、不要旁白配音。

## 何时使用

- 用户或批量任务要求同时写 `video_prompt` 和 `image_prompt`
- 只画该段落 **第一个【镜头N】** 的瞬间，不要把整段 8–10 秒的动作压进一张图

## 输出结构（单段连贯中文，给 Gemini 看）

`image_refs` 顺序固定：场景空镜 → 角色设定图 → 道具单品。文字编号必须跟这个顺序一致。

```
这是同一部短片里的一镜，不是另一部影片。全片保持[项目视觉风格]。
根据前面按顺序附上的参考图，生成一张 16:9 单帧分镜静帧。
第一张图是场景空镜（[名]）：只用它的空间布局与陈设，把人物放进这个空间，不要另造场景。
第二张图是角色设定（[名]）：必须保持同一张脸、发型与服装，只改这个镜头的姿势与景别，禁止换人换脸。
第三张图是道具（[名]）：保留包装、Logo 与比例，放到画面里正确的位置。
（如有）第 N 张图是本片已生成的分镜：保持同一画风、色温、服装；只改这一镜的动作与景别。
画面：[第一个子镜头谁在哪、做什么、景别]
光线：[氛围；色温必须跟全片一致]
不要字幕、不要时间轴、不要配音旁白
```

没有某类参考图就删掉对应那一行，不要假装有图。

## 硬约束

- 全部分镜静帧必须像**同一部短片**：同一画风、色温、镜头质感、服装与发型。禁止每镜另造一张脸、一套衣服或另一种媒介
- 若前面附上「本片已生成分镜」，只改这一镜的动作、景别与机位，不要换成另一部电影
- 有人物图时，脸必须来自「第 N 张图」那张像素，不要用外貌文字重新发明一张脸
- 角色参考图可能是三视图／设定图：只取同一张脸与服装
- 场景参考图是空镜：不要另造一间办公室
- 没有附上的绑定项才用文字外貌补
- 禁止只写 `@角色名` 或 `<IMAGE_REF_N>` 当锁定方式

## 禁止事项

- 把 `video_prompt` 原样复制成 `image_prompt`
- 时间轴分段、运镜过程、「女声旁白／男声旁白」
- 屏幕上的可读字幕、标题、提示词原文
- 忽略前面附上的人物／场景／道具图

## 保存

调用 `update_storyboard` 时可同时传 `storyboard_id`、`video_prompt`、`image_prompt`。不要改其他字段。
